using System;
using System.Collections.Generic;

namespace num_finder
{
    class Alpha                               // entry point
    {
        static void Main(string[] args)
        {
            // Console.Clear();                 // clearing the result window
            // System.Console.WriteLine();
            // System.Console.WriteLine("CHOOSE THE LEVEL : press ( 0 for easy / 1 for medium / 2 for hard) : ");
            // Level l = new Level(); 
            // string level = Console.ReadLine();
            // l.hardShip(level);               // passing the level (easy/medium/hard)
            // Get g = new Get();
            // g.getStart();                   
            // g.getEnd();
            // g.go();

            Test.testHackerRank();
        }
    }
}
